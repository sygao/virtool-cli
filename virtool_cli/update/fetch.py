import asyncio
import json
from pathlib import Path


def run(otu_path: str, verbose=False):
    """
    Runs the asynchronous routines to find new isolates for all OTU in a reference

    :param src: Path to a reference directory
    """
    asyncio.run()
